# Supabase Auth (client-side)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Database — Vercel + Supabase integration adds these automatically:
# POSTGRES_PRISMA_URL
# POSTGRES_URL_NON_POOLING
#
# For local dev, copy from Supabase → Settings → Database → Connection string:
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
